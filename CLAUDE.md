# CLAUDE.md

## Project Overview

**Claude-test** is a newly initialized repository. This file serves as the guide for AI assistants (including Claude Code) working in this codebase.

## Repository Structure

```
Claude-test/
├── README.md        # Project readme
└── CLAUDE.md        # This file — AI assistant guide
```

## Development Setup

This project is in its initial state. As the project grows, document setup steps here (e.g., dependency installation, environment variables, database setup).

## Common Commands

_No build, test, or lint commands are configured yet._ Update this section as tooling is added.

<!-- Example format once tools are configured:
| Task       | Command          |
|------------|------------------|
| Install    | `npm install`    |
| Build      | `npm run build`  |
| Test       | `npm test`       |
| Lint       | `npm run lint`   |
| Format     | `npm run format` |
-->

## Code Conventions

_No conventions established yet._ As the project takes shape, document:

- Language and framework choices
- File and folder naming conventions
- Code style (formatting, linting rules)
- Import ordering preferences
- Error handling patterns
- Testing expectations (unit, integration, e2e)

## Git Workflow

- **Default branch:** `master`
- Write clear, descriptive commit messages
- Use feature branches for new work

## Key Architecture Decisions

_None yet._ Record significant design decisions here as the project evolves (e.g., chosen frameworks, state management approach, API design patterns).

## AI Assistant Guidelines

When working in this repository:

1. **Read before modifying** — always read a file before proposing changes to it.
2. **Keep changes minimal** — only change what is necessary to fulfill the request.
3. **Don't over-engineer** — avoid adding abstractions, utilities, or patterns beyond what is needed.
4. **Update this file** — when adding new tooling, commands, conventions, or architecture decisions, update this CLAUDE.md to keep it current.
5. **Respect existing patterns** — as the codebase grows, follow the conventions already in use rather than introducing new ones.
